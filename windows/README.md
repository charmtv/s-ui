# Windows 使用说明

## 安装

1. 下载并解压 Windows 发布包。
2. 右键以管理员身份运行 `install-windows.bat`。
3. 使用 `s-ui-windows.bat` 管理服务。

## 卸载

以管理员身份运行：

```bat
uninstall-windows.bat
```

## 本地构建

CMD：

```bat
build-windows.bat
```

PowerShell：

```powershell
.\build-windows.ps1
```
